<!-- A simplified configuration file for the basic deployment of openGauss on a single node -->
<!-- Do not copy and use as is -->
<!-- Be sure to modify the configurations based on requirements -->
<!-- Values between two curly brackets {{}}, e.g. {{192.168.0.1}} must be replaced with your own values according to specifications and requirements -->


<?xml version="1.0" encoding="UTF-8"?>
<ROOT>
    <!-- Overall information about openGauss -->
    <CLUSTER>
        <!-- Database name -->
        <PARAM name="clusterName" value="{{huawei}}" />
        <!-- Database node name (hostname) -->
        <PARAM name="nodeNames" value="{{db}}" />
        <!-- Database installation directory -->
        <PARAM name="gaussdbAppPath" value="/opt/huawei/install/app" />
        <!-- Log directory -->
        <PARAM name="gaussdbLogPath" value="/var/log/omm" />
        <!-- Temporary file directory -->
        <PARAM name="tmpMppdbPath" value="/opt/huawei/tmp" />
        <!-- Database tool directory -->
        <PARAM name="gaussdbToolPath" value="/opt/huawei/install/om" />
        <!--Directory of the core file of the database -->
        <PARAM name="corePath" value="/opt/huawei/corefile" />
        <!-- Node IP address, which corresponds to the database node name list -->
        <PARAM name="backIp1s" value="{{192.168.0.1}}"/> 
    </CLUSTER>
    <!-- Information about the node deployment on each server -->
    <DEVICELIST>
        <!-- Deployment information on node 1 -->
        <DEVICE sn="{{db}}">
            <!-- Host name of node 1 -->
            <PARAM name="name" value="{{db}}"/>
            <!-- AZ where node1 is located and priority of the AZ -->
            <PARAM name="azName" value="AZ1"/>
            <PARAM name="azPriority" value="1"/>
            <!-- IP address of node 1. If only one NIC is available for the server, set backIP1 and sshIP1 to the same IP address. -->
            <PARAM name="backIp1" value="{{192.168.0.1}}"/>
            <PARAM name="sshIp1" value="{{192.168.0.1}}"/>
               
	    <!--dbnode-->
	    <PARAM name="dataNum" value="1"/>
	    <PARAM name="dataPortBase" value="{{15400}}"/>
	    <PARAM name="dataNode1" value="/opt/huawei/install/data/dn"/>
            <PARAM name="dataNode1_syncNum" value="0"/>
        </DEVICE>
    </DEVICELIST>
</ROOT>